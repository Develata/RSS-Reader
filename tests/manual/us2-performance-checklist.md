# US2 性能检查模板：10,000 篇文章下的状态切换与搜索

## 记录信息

- 验证日期：`2026-03-25`
- 验证环境：`WSL Ubuntu + Dioxus Desktop (WSLg)`
- 构建方式：`自动化测试已通过；并在真实桌面会话中完成状态切换、筛选、搜索和重启持久化验证`
- 文章总量：`未达到 10,000 篇，当前样本为真实订阅拉取结果`
- 未读文章量：`已验证存在`
- 收藏文章量：`已验证存在`

## 建议前置条件

- 本地数据库内已有约 10,000 篇文章。
- 数据中同时包含已读、未读、已收藏和未收藏文章。
- 至少准备 3 个可命中的标题关键字和 1 个无结果关键字。

## 建议记录方式

- 状态切换与筛选切换至少执行 10 次，记录是否出现明显停顿或批量错乱。
- 搜索建议记录首个结果稳定出现所需时间。
- 如不能精确计时，也要记录“无明显停顿 / 轻微停顿 / 明显停顿”。

## 检查步骤

1. 打开文章页，在默认“全部文章”视图下连续滚动，确认大数据量下列表仍可交互。
2. 选择一篇未读文章切换为已读，记录状态反馈时间。
3. 选择一篇未收藏文章切换为收藏，记录状态反馈时间。
4. 连续对 10 篇文章执行已读切换，确认 UI 和持久化结果一致。
5. 连续对 10 篇文章执行收藏切换，确认 UI 和持久化结果一致。
6. 启用“仅未读”筛选，记录列表稳定更新时间。
7. 启用“仅收藏”筛选，记录列表稳定更新时间。
8. 在筛选开启状态下输入高命中关键字，记录搜索结果更新时间。
9. 输入低命中关键字和无结果关键字，确认无异常卡顿或错误。
10. 进入阅读页，使用快捷键 `M`、`F` 连续切换状态后返回列表，确认状态同步正常。
11. 重启应用后抽查多篇文章，确认状态持久化无误。

## 结果记录

| 项目 | 测量值/现象 | 结论 | 备注 |
|------|-------------|------|------|
| 默认列表滚动 | 当前样本下无明显卡顿 | 通过 | 未在 10,000 篇规模下测量 |
| 单篇已读切换 | 可立即生效 | 通过 | 列表与阅读页状态一致 |
| 单篇收藏切换 | 可立即生效 | 通过 | 列表与阅读页状态一致 |
| 10 篇已读批量切换 | 未执行 | 未完成 | 当前未做大样本批量切换记录 |
| 10 篇收藏批量切换 | 未执行 | 未完成 | 当前未做大样本批量切换记录 |
| 仅未读筛选切换 | 可正常工作 | 通过 | 当前样本下无明显停顿 |
| 仅收藏筛选切换 | 可正常工作 | 通过 | 当前样本下无明显停顿 |
| 高命中标题搜索 | 可正常返回结果 | 通过 | 当前样本下无明显停顿 |
| 无结果标题搜索 | 未单独记录 | 部分验证 | 未报告异常 |
| 阅读页快捷键回写 | `M`/`F` 可用 | 通过 | 与按钮行为一致 |
| 重启后状态持久化 | 已验证保持 | 通过 | 已读/收藏重启后仍在 |

## 通过标准

- 在约 10,000 篇文章规模下，列表浏览、筛选切换和标题搜索保持可响应。
- 已读/收藏状态切换与列表、阅读页显示一致，且重启后不丢失。
- 结果满足 [spec.md](/home/develata/gitclone/RSS-Reader/specs/001-minimal-rss-reader/spec.md) 中 `SC-002` 的目标，即交互过程中无明显停顿。

## 结论

- 状态：`部分完成`
- 主要观察：`真实桌面会话下，已读/收藏、筛选、搜索、快捷键和重启持久化均可用。`
- 风险与后续动作：`本清单原目标是 10,000 篇规模性能验证；当前尚未补齐大样本数据与批量切换测量，因此不能视为完整性能验收。`
