# v0.1.16 tag 与发布核验

- 日期：2026-09-23
- 作者 / Agent：Codex
- 分支：`main`
- 当前 HEAD（发布源码）：`ce986d779c07f98561915742300fe2ee6446c1ba`
- 相关 commit：`commit: pending`（本记录写于 tag 之后，最终提交见 Git 历史）
- 相关 tag / release：[`v0.1.16`](https://github.com/Develata/RSS-Reader/releases/tag/v0.1.16) annotated tag，指向 `ce986d7`
- 状态：`released`

## 工作摘要

用户要求推送新 tag。确认 `main` 处于干净工作树、发布源码对应的 21 个 CI job 与 Docker workflow 全绿、上一个版本为 `v0.1.15` 后，创建并推送 `v0.1.16`。

## 影响范围

- 模块：Git tag、GitHub Release、GHCR；本文件及后续 README 发布状态更新。
- 平台：Windows、Linux、macOS、Android、Web、Docker。
- 额外影响：tag push 自动触发 `.github/workflows/release.yml` 与 Docker workflow。

## 关键变更

- 远端 annotated tag `v0.1.16` 解引用后为完整 commit `ce986d779c07f98561915742300fe2ee6446c1ba`；Android tag 解析得到 `versionCode=116`、`versionName=0.1.16`。
- Docker tag workflow 成功，日志报告 `ghcr.io/develata/rss-reader:v0.1.16`、`sha-ce986d7`、`latest` 指向同一 digest `sha256:0503ddf3d955777360c807c34c5bda32b9e9a68c9a42d00d374ba6104e666a20`。
- Release workflow 的 Windows、Linux、Android、Web、macOS x64 与 arm64 构建和最终发布 job 均成功。公开 Release 非 draft、非 prerelease，11 个构建附件已上传。已补写本版修复、验证范围及 Linux 包和设备验收缺口。
- 中英文 README 及使用指南同步到公开版本 `v0.1.16`；继续明确 Linux `.deb` 不能据包结构检查推断普通用户首次启动可用。

## 验证与验收

### 自动化验证

- [发布源码主线 CI](https://github.com/Develata/RSS-Reader/actions/runs/35899555616)：21/21 job 成功。
- [tag Docker workflow](https://github.com/Develata/RSS-Reader/actions/runs/35902496227)：成功；实际 push digest 见上。
- [tag Release workflow](https://github.com/Develata/RSS-Reader/actions/runs/35902496443)：7/7 job 完成且成功。
- `git ls-remote origin refs/tags/v0.1.16 refs/tags/v0.1.16^{}`：远端 tag 身份一致。
- `gh release view` / GitHub Release API：确认 `v0.1.16` 为正式公开 Release，版本说明已写入且有 11 个附件。
- `gh release download v0.1.16`：下载 11 个附件到忽略的 `target/`，逐项比对实际文件大小及 SHA-256 与 GitHub API `size`、`digest`，11/11 一致。
- `dpkg-deb -c`：本版 Linux `.deb` 仍把 `rssr-app` 安装至 `usr/bin`，确认已知写权限问题并未消失。
- `git diff --check`、`cargo fmt --all --check`、README / 使用指南版本与附件映射检查：均通过。
- 本轮未重跑本地全量 Rust 检查；使用同一 commit 的远端主线 CI 结果。

### 手工验收

- Windows 原生右键菜单实际弹出、Android/macOS 实机交互：本轮未执行，既有缺口保持。
- Linux `.deb` 普通用户首次启动：未验；现有源码的数据目录仍位于可执行文件旁，该包安装到 `/usr/bin`，已知写权限问题未解决。

## 结果

- `v0.1.16` 已正式发布；tag 固定在 `ce986d7`，本记录与文档修正为 tag 之后的主线提交，不改变发布源码。

## 风险与后续事项

- Linux `.deb` 不应被描述为普通用户已经可用；修复需单独处理数据目录与已有数据迁移。
- CI 和包构建不能代替 Android/macOS/Windows 的未完成设备交互验收。

## 给下一位 Agent 的备注

- 不要移动或重建已经推送的 `v0.1.16` tag；后续代码和文档改动属于新的 `main` 提交，不能回写为本版发布源码。
