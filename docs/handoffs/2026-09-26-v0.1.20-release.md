# v0.1.20 发布

- 日期：2026-09-26
- 作者 / Agent：Codex
- 分支：main
- 发布源码：2b69c3bcfe801570c11d0ddd61fca583578d0d61
- 相关 commit：发布源码 2b69c3b；本交接和 README 附件映射归入本记录所在的后续文档提交
- 相关 tag / release：v0.1.20，annotated tag；tag 对象 `ed62af12e0225af42901d380179a7f8fbd47c14e`
- 状态：`released`（发布与附件校验完成）

## 工作摘要

用户在已知真实远端代理与 Android 设备交互验收缺口后明确授权打新 tag。本轮将 v0.1.20 固定于已通过完整主线检查的 2b69c3b，跟进跨平台安装包与 Docker 发布，并保留上述验证边界；不移动旧 tag，不修改发布源码。

## 影响范围

- 模块：阅读元信息与本地时间、来源未读数、会话位置恢复、批量已读、网站 feed 发现、刷新新增计数、折叠导航与浏览器存储一致性。
- 平台：Windows、Linux、macOS Intel / Apple Silicon、Android、Web、Docker 与 CLI。
- workflow / 文档：沿用既有 Release / Docker 工作流；发布说明、中英文 README 附件映射及本交接。
- 契约：本轮发布操作无源码、trait、端口、数据库 schema 或 `data-*` 变更；版本内容见 v0.1.19...v0.1.20 提交对比和对应功能交接。

## 关键变更

- `git tag -a v0.1.20 2b69c3bcfe801570c11d0ddd61fca583578d0d61 ...` 创建附注 tag，随后仅推送 `refs/tags/v0.1.20`。
- Release 说明包含主要功能、Web 多标签页升级提示、8 MiB 响应上限和设备 / 网络验收缺口。
- 发布产物、日志与 SHA-256 核对记录保存在忽略目录 `target/release-v0.1.20/`。

## 验证与验收

### 自动化验证

- `git fetch origin`、tag 创建、`git push origin refs/tags/v0.1.20`：均退出 0。
- `git ls-remote origin refs/tags/v0.1.20 'refs/tags/v0.1.20^{}'`：退出 0，解引用为发布源码完整 SHA。
- [主线 CI 36249723692](https://github.com/Develata/RSS-Reader/actions/runs/36249723692)：2b69c3b 的 21/21 job 成功；[主线 Docker 36249723704](https://github.com/Develata/RSS-Reader/actions/runs/36249723704) 成功。
- [tag Release 36250488378](https://github.com/Develata/RSS-Reader/actions/runs/36250488378)：7/7 job 成功（Windows、Linux、macOS ×2、Android、Web、publish），含 Linux `.deb` 安装后普通用户两次启动与数据库复用检查。
- 首次 `gh run watch` 因 GitHub API 连接 EOF 退出 1，属于本地监控中断；最终再次执行 `gh run watch 36250488378 --exit-status --interval 30` 退出 0，与完整 run / job API 结果一致。
- [tag Docker 36250488382](https://github.com/Develata/RSS-Reader/actions/runs/36250488382)：成功，`gh run watch ... --exit-status --interval 30` 退出 0；已推送 v0.1.20、sha-2b69c3b、latest。
- 发布 tag 的 Docker manifest digest：`sha256:d87913d3a8856640293565177f6b3ffcde72eb3fe7e8ff739b0600f2ba14dbdb`。公开 registry 中的 v0.1.20 与 latest 均与工作流日志匹配，平台为 linux/amd64，另有 provenance attestation。
- 本机首次 `docker buildx imagetools inspect` 退出 1，原因是现有 Docker Desktop credential helper 在 WSL 调用失败；后续使用独立空 `DOCKER_CONFIG` 做公开镜像只读检查，两次均退出 0，不修改用户 Docker 配置。
- `timeout 600s gh run download 36250488378 --name release-android --name release-linux --name release-windows --name release-web --dir target/release-v0.1.20/ci-artifacts`：退出 0；先并行于构建取得 7 个附件，随后按公开 Release API 核对内容身份，不重复下载相同文件。
- `gh release download v0.1.19 --pattern RSS-Reader-android-arm64-v8a-release.apk ...`、新旧 APK 的 `apksigner verify --print-certs` / `aapt dump badging`：退出 0；签名证书 SHA-256 与包名一致，versionCode 119 → 120，versionName 为 0.1.20，无 debug 签名。
- `dpkg-deb -f ... Version Architecture Depends` 及包元数据断言：退出 0；Version 0.1.20，Architecture amd64，包含 WebKit 运行库依赖。
- `timeout 600s gh release download v0.1.20 --skip-existing --dir target/release-v0.1.20/assets`：退出 0，下载其余 4 个 macOS 附件；前述 7 个 CI 附件保留原始字节用于统一校验。
- `python3 target/release-v0.1.20/verify_assets.py`：退出 0，11/11 附件名称、上传状态、文件大小和 SHA-256 与公开 Release API 一致；清单为 `asset-sha256.txt`。Web、macOS、Windows 与 CLI 归档入口断言也退出 0。
- `gh release edit v0.1.20 --title ... --notes-file target/release-v0.1.20/release-notes.md` 及回读正文一致性断言：退出 0，标题和完整发布说明已生效。
- `git diff --check`：退出 0。未在本地重跑 cargo / wasm harness：本轮只做 tag、发布核验与文档更新，发布源码的完整主线 CI 和 Release 检查已经通过。

### 手工验收

- 本轮未运行 Android / Windows / macOS 真机交互；Android 当前无连接设备或已配置模拟器，另两平台无本轮可用的实机验收环境。
- 真实远端 `/feed-proxy` 的正常 DNS 网络验收未补：前轮在 Fake-IP DNS 下返回预期安全拒绝；同源 fixture 验收不替代该项。
- 之前 Web 360×800 / 1280×800、五种阅读主题及真实浏览器交互结果见 [发布准备交接](2026-09-26-push-and-release-readiness.md)，本轮没有把构建通过当作设备验收。

## 结果

- tag、版本镜像与 [GitHub Release](https://github.com/Develata/RSS-Reader/releases/tag/v0.1.20) 已公开发布。Release 在 2026-09-26 15:19:51 UTC 创建，非 draft、非 prerelease，共 11 个附件。
- 中英文 README 的当前附件映射更新为 v0.1.20，保留 Android / macOS 实机验收边界；Release 说明保留真实代理验收缺口与 Web 升级提示。

## 风险与后续事项

- 保留 Android 系统返回、选择手柄、图片手势与正常 DNS 网络远端代理的验收缺口。
- Linux 包依赖来自 Ubuntu 24.04，不推定旧发行版兼容；Android 卸载会删除本地数据。
- 升级 Web 后重新加载全部旧标签页；需 HTTPS / localhost 和 Web Locks。

## 给下一位 Agent 的备注

- 发布源码固定于 2b69c3b；本轮最终文档提交位于 tag 之后，不回移 tag。
- 先查看上述工作流与 Release 附件校验记录；避免只凭主线 CI 推断发布成功。
