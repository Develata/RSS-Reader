# v0.1.19 发布

- 日期：2026-09-24
- 作者 / Agent：Claude Code (Opus)
- 分支：main
- 发布源码：4a39d43a695244cc2e59fbd9b36d15edad46377a
- 相关 commit：4d97613、1989ce6、40fcff9、56b5c44、4a39d43；本文与 README 附件映射归入随后的文档提交
- 相关 tag / release：[v0.1.19](https://github.com/Develata/RSS-Reader/releases/tag/v0.1.19)（annotated tag，git 默认用户 Develata，解引用为上述完整 SHA）
- 状态：`released`

## 工作摘要

按 Develata 授权：换导航图标后 push，检查通过后打 tag 并 push。实现与本地验证见 `2026-09-24-ci-critical-path-and-entry-actions.md`。

## 影响范围

- 平台：Windows、Linux、macOS、Android、Web、Docker。
- 内容：文章列表动作层级、导航图标、筛选开关文字、wasm Clippy 修复、CI 关键路径。

## 验证与验收

### 自动化验证

- [主线 CI](https://github.com/Develata/RSS-Reader/actions/runs/36046727528)：21/21 job 成功，墙钟 321 s。
  - 所有 job 的 `Cleanup runner` 为 0 s（空闲空间充足，跳过）；`native-module (rssr-app)` 由 302 s 降至 113 s。
  - `android-smoke` 316 s，与之前持平：本次为 Gradle 缓存首跑（miss），已保存 454 MB 缓存；命中后的收益待下一次运行确认。
- [主线 Docker](https://github.com/Develata/RSS-Reader/actions/runs/36046727621)：成功。
- [tag Release](https://github.com/Develata/RSS-Reader/actions/runs/36047468766)：7/7 job 成功（Windows、Linux、macOS ×2、Android、Web、publish），含 Linux `.deb` 安装及普通用户启动 smoke。
- [tag Docker](https://github.com/Develata/RSS-Reader/actions/runs/36047468568)：成功。
- `gh release download v0.1.19`：11/11 附件大小与 SHA-256 与 Release API digest 一致；Release 非 draft、非 prerelease。
- `.deb`：`Version: 0.1.19`，`Depends` 含 GTK、JavaScriptCoreGTK、libsoup 等运行库。
- 发布 Web 包的 wasm 中确认含 RSS / 滑杆图标路径、`entry-controls-toggle-label`、新动作按钮 selector。
- `git ls-remote origin 'refs/tags/v0.1.19^{}'` 解引用为发布 SHA。

### 手工验收

- 未进行 Windows / macOS / Android 真机交互验收。
- README 截图 `assets/readme/rss-reader-overview.png` 仍显示旧 R / S / ⚙ 顶栏，未更新。

## 结果

- v0.1.19 已公开发布；发布说明含界面变化、自定义主题注意事项、CI 变化与验证范围。
- 中英文 README 的当前附件映射更新为 v0.1.19。

## 风险与后续事项

- 使用自定义 CSS 改过 `.button[data-variant="secondary"]` 的用户，文章列表动作按钮不再受该规则影响，需要改用新 selector（已在发布说明与主题作者参考中注明）。
- 下次 CI 观察 Gradle 缓存命中后 android-smoke 的实际耗时。
- 更新 README 截图。
