# v0.1.15 正式发布

- 日期：2026-09-22
- 作者 / Agent：Codex
- 分支：`main`
- 当前 HEAD（发布源码）：`f0226f1ab6ec44e05287a4932aa0718d1b9481ed`
- 相关 commit：发布源码 `f0226f1`；本记录的提交见该文件的 Git 历史
- 相关 tag / release：[`v0.1.15`](https://github.com/Develata/RSS-Reader/releases/tag/v0.1.15)
- 状态：`released`

## 工作摘要

用户明确授权直接发布 `v0.1.15`，并决定本轮不等待 Android 真机测试。已将 `f0226f1` 打成 annotated tag、推送，等待 Release 与 Docker 工作流完成，并复验公开产物。

## 影响范围

- 模块：`docs/handoffs/` 记录本次发布；产品源码未在发布过程中修改。
- 平台：Windows、macOS、Linux、Android、Web、Docker。
- 额外影响：GitHub tag、GitHub Release、GHCR 容器标签。

## 关键变更

### 发布身份与产物

- 远端 annotated tag `v0.1.15` 解引用后指向完整 commit `f0226f1ab6ec44e05287a4932aa0718d1b9481ed`。
- [Release workflow](https://github.com/Develata/RSS-Reader/actions/runs/35775878777) 的 macOS arm64、macOS x64、Linux、Windows、Android、Web 构建及发布 job 均成功；Release 为正式发布，非 draft、非 prerelease。
- 公开 Release 共 11 个构建资产：Android APK/AAB，Linux 桌面包与 CLI，macOS 两种架构的桌面包与 CLI，Windows 桌面包与 CLI，Web 包。已补写本版主要变化、验证范围和未做的设备验收。
- [Docker workflow](https://github.com/Develata/RSS-Reader/actions/runs/35775878758) 成功；工作流日志确认推送 `ghcr.io/develata/rss-reader:v0.1.15`、`sha-f0226f1`、`latest`，共同 digest 为 `sha256:8d18384a8a72e0ab3432c103b4bce2fd9060279af927199a9d9f521fa468a8d7`。

### Android 签名边界

- Android 发布 job 报告包名 `io.github.develata.rssreader`、`versionCode=115`、`versionName=0.1.15`，`apksigner verify --print-certs` 通过。
- 本版签名证书 SHA-256 为 `4ed80b39e18b306e7072cbe258c7adf99ea9223d9026a81f9e78a6fb1f95dc17`，与 [既有发布签名交接记录](2026-07-27-android-release-signing.md) 中独立复验的长期签名身份一致。

## 验证与验收

### 自动化验证

- [发布前 `main` CI](https://github.com/Develata/RSS-Reader/actions/runs/35772037425)：完成，成功；发布源码 commit 为 `f0226f1`。
- [Release workflow](https://github.com/Develata/RSS-Reader/actions/runs/35775878777)：完成，7/7 job 成功。
- [Docker workflow](https://github.com/Develata/RSS-Reader/actions/runs/35775878758)：完成，成功；镜像推送日志显示三个标签与同一 digest。
- `gh release download v0.1.15`：下载全部 11 个构建资产，逐个核对实际字节数和 SHA-256 与 GitHub Release API 公布的 `size`、`digest`，11/11 一致。下载文件位于本机忽略的 `target/` 下，未纳入提交。
- `gh release view` / GitHub Release API：确认 tag、公开状态、说明和资产。
- 本轮没有重新运行本地 `cargo test`、Clippy 或 UI 测试；使用同一发布源码 commit 已成功完成的主线 CI 和发布工作流结果。

### 手工验收

- GitHub Release 公开页面及发布说明：已检查。
- Android 真机安装、返回键、下拉刷新、长按选择和图片手势：未执行；用户明确要求直接发布、不等待 Android 真机测试。
- macOS 实机交互：未执行；当前仅有 Windows 主机可测。Windows/Web 的发布前验收见此前本日交接记录和主线 CI。

## 结果

- `v0.1.15` 已正式发布；tag 固定在 `f0226f1`。本交接记录在 tag 之后单独提交，不改变已发布源码。
- Android 构建、版本信息和签名检查通过；这些证据不等同于真机交互验收。

## 风险与后续事项

- Android 真机及 macOS 实机用户路径尚未复验，后续获得设备时应补做，尤其关注系统返回、触摸选择、下拉刷新及图片查看。
- 当前 GitHub token 查询 GHCR 包版本 API 返回 403（缺少 `read:packages`），故 Docker 发布证据来自成功的 workflow 及其实际 push digest 日志，未独立列出 registry 版本。

## 给下一位 Agent 的备注

- 发布源码与交互变更以 `v0.1.15` / `f0226f1` 为准；本记录是 tag 之后的文档提交。
- 继续跨端验收前，先看本日 UI、原生验收与 CI 交接记录；不要把 CI 构建通过写成 Android/macOS 实机体验已验证。
