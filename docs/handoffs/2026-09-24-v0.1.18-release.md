# v0.1.18 发布与边界回归验证

- 日期：2026-09-24
- 作者 / Agent：Codex
- 分支：main
- 发布源码：2652c5b8ea02904e3f62411eb7fef6609a2f1025
- 相关 commit：4a8b70a、1ad4297、145eccd、2652c5b；本文与发布后文档归入包含本文件的文档提交
- 相关 tag / release：[v0.1.18](https://github.com/Develata/RSS-Reader/releases/tag/v0.1.18)（annotated tag 指向上述完整 SHA）
- 状态：`released`

## 工作摘要

按用户授权执行 push、等待同一提交 CI、发布补丁版本，并补查 SQLite 升级与 Web 部署边界。没有改动产品实现或移动已有 tag。

## 影响范围

- 模块：Git tag、GitHub Release、Docker、发布说明与使用文档。
- 平台：Windows、Linux、macOS、Android、Web。
- 修复内容与本地回归见 `2026-09-24-global-boundary-review.md`。

## 关键变更

- 推送四个审查提交后，确认 CI 和 Docker 对完整发布 SHA 成功，再创建并推送 v0.1.18。
- 发布说明列出环境代理行为变化、默认数据库布局不变、异常 URL 的旧数据不自动恢复，以及真机验收缺口。
- 发布后文档更新不改变 tag 所指源码。

## 验证与验收

### 自动化验证

- [主线 CI](https://github.com/Develata/RSS-Reader/actions/runs/35990937796)：21/21 job 成功，包含 native 各模块、wasm、Web release 构建、主题 UI、Android 构建、工具及汇总门禁。
- [主线 Docker](https://github.com/Develata/RSS-Reader/actions/runs/35990937885)：成功。
- [tag Release](https://github.com/Develata/RSS-Reader/actions/runs/35991688858)：成功。
- [tag Docker](https://github.com/Develata/RSS-Reader/actions/runs/35991688935)：成功。
- `cargo build --locked -p rssr-cli` / `cargo build --locked -p rssr-web`：通过。
- 临时旧版工作树使用 b61aded（与 v0.1.17 的 infra/CLI 源码无差异）。旧 CLI 在中文/空格临时目录创建正常双库，fixture 写入订阅、文章、已读/收藏、HTML 正文；新版打开后这些值保持，两个数据库 integrity_check 均为 ok。
- 特殊 URL `sqlite://.../mode=memory.db?mode=rwc`：旧 CLI 在正文迁移阶段拒绝启动（已有索引 migration 2 不在正文 migrations 中）；新版能打开同一索引并建立独立正文库。没有证据表明旧版正常完成过该 URL 的双库初始化，也没有对用户数据库执行迁移或修复。
- 临时检查脚本 `/tmp/rssr-v018-upgrade-check.py`。首次共享 Cargo target 的切换复用了旧二进制；定向清理两个受影响 crate 的构建产物后重新编译当前源码，才取得新版验证结果。
- 实际启动新编译的 `rssr-web`，在临时静态目录及认证状态下请求 HTTP 登录；正常 `/feeds` 保持，反斜杠、Tab、CRLF 回跳变为 `/`，四例均返回 303 和会话 cookie。结束后关闭进程并清理临时状态。
- `run_rssr_web_proxy_feed_smoke.sh --release --skip-build`：失败，未计为通过。本机 ruanyifeng.com 解析为 fake-IP `198.18.0.19`；本地绕过 curl 环境代理并换公开 feed 后仍被内网/本地地址规则拒绝。该路径没有为发布而放宽。静态包只作为 host fixture，不作为新 UI 构建证据；新 UI 构建证据来自上述 CI。

- `gh release download v0.1.18` 下载公开附件，11/11 文件大小及 SHA-256 与 Release API 匹配；校验清单保存于本机下载目录的 `verified-assets.json`。
- 下载 `.deb` 的 `Version=0.1.18`，`Depends` 包含 WebKitGTK、GTK、libsoup、libxdo 等实际运行库；SHA-256 为 `3fda6c8cc6851a7179589eda4bcabc82d1e588f82c5ef0bda5c049a4eaffce0c`。
- 下载的 Linux CLI 在隔离中文/空格目录运行 `--help` 和特殊 URL 的 `list-feeds`，退出成功，实际创建独立索引/正文库，两者 integrity_check 均为 ok。
- `git ls-remote` 核对远端 annotated tag 解引用为完整发布 SHA；`git diff --check` 通过。

### 手工验收

- 未进行新的 Windows 原生右键菜单、Android 手势/系统返回、macOS 实机交互验收；CI 构建不能代替这些路径。
- 公开 feed 经实际部署网络的端到端成功结果缺失；隔离子进程固定 IP 连接测试通过，不等价于该检查。

## 结果

- Release 7/7 job 成功，公开版本非 draft、非 prerelease，11 个附件均已上传；发布说明已补写。
- Docker 版本镜像 digest：`sha256:12dd0374a8aff3e98303eadd05ee2dc1513a4ebae965ab4af39e7f58bb5af047`。
- Linux job 成功执行本版 `.deb` 安装及普通用户两次启动，验证用户数据目录与数据库复用。
- 中英文 README 的当前附件映射更新至 v0.1.18；v0.1.17 历史修复说明保留版本语境。Web 部署说明补充 fake-IP DNS 限制。

## 风险与后续事项

- Web host 需能直连公开 feed；环境代理及 fake-IP DNS 部署需调整网络配置。不会绕过 SSRF 地址限制。
- 异常自定义数据库布局没有自动恢复机制；正常布局的升级已做隔离 fixture 验证。
- 未进行性能测量，不宣称性能提升。

## 给下一位 Agent 的备注

- 仅使用上述完整 SHA、tag 和本版附件核对发布身份；不要用上一版绿色流水线替代本次验证。
