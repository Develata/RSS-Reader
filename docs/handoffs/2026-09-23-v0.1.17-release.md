# v0.1.17 发布与包验收

- 日期：2026-09-23
- 作者 / Agent：Codex
- 分支：`main`
- 当前 HEAD（发布源码）：`5780056b828c4245c615b6e6a15d62f44ab67c83`
- 相关 commit：`d069e2f`、`329da03`、`5780056`；本记录和 README 更新 `commit: pending`
- 相关 tag / release：[`v0.1.17`](https://github.com/Develata/RSS-Reader/releases/tag/v0.1.17)，annotated tag 指向上述完整 commit
- 状态：`released`

## 工作摘要

在 Linux `.deb` 数据目录和依赖、CLI 刷新参数、Web 代理校验的修复已进入 `main` 后，复核主线 CI，修正一处浏览器验收的无界动画帧等待，发布 `v0.1.17` 并核对实际附件。

## 影响范围

- 模块：浏览器固定视口验收脚本；Git tag、GitHub Release、GHCR；中英文 README 和使用指南。
- 平台：Windows、Linux、macOS、Android、Web、Docker。
- 额外影响：tag push 触发 Release 与 Docker workflow；本轮没有改 application/domain 或平台业务分支。

## 关键变更

### 发布前浏览器验收修复

- `329da03` 的主线 CI 中，默认主题前九项浏览器断言通过，无 console error；随后在连续两次 `requestAnimationFrame` 等待中发生 `Runtime.evaluate#14` 的 15 秒 CDP 超时。其它四组主题 UI job 通过。
- `5780056` 为两处帧等待加 1 秒有限回退；若帧不返回，后续仍会按真实样式断言，避免无限等帧。用 CI 上传的同一 Web bundle，在隔离 worktree 中配合本地 Chromium 重跑默认主题完整 smoke，通过；新 commit 的主线 CI 默认主题和其它主题均通过。

### 发布身份与产物

- 发布前 `main`/`origin/main` 与 tag 解引用均为 `5780056b828c4245c615b6e6a15d62f44ab67c83`，工作树干净；没有移动已发布的 tag。tag 推送后的 Docker workflow 将 `v0.1.17`、`sha-5780056`、`latest` 推至同一 digest `sha256:a27447061dfef0fba19a0c13d82839211ce8900a1a2d7dfc44f1cea28a2b8e38`。
- Release workflow 首次运行仅 Android 预取 Gradle 失败：下载重定向返回自签名证书，`curl` 退出 60，尚未构建 APK。整轮结束后对同一 tag 重跑失败 job，Android 正式签名 APK/AAB 与最终发布 job 均成功；其它平台成功 job 没有重建，发布源码身份未变。
- 公开 Release 非 draft、非 prerelease，11 个附件均已上传，并补写本版说明。Linux `.deb` 的 `Version=0.1.17`，`Depends` 包含 WebKitGTK、GTK、libsoup、libxdo；本版 `.deb` 的 SHA-256 为 `3ef6d023fc949baf3b62d0d8feeae692fcd73f1ea7123e72a19faa6f378933b5`。

## 验证与验收

### 自动化验证

- [发布源码主线 CI](https://github.com/Develata/RSS-Reader/actions/runs/35916061695)：21/21 job 成功；[主线 Docker](https://github.com/Develata/RSS-Reader/actions/runs/35916061731) 成功。
- [tag Docker](https://github.com/Develata/RSS-Reader/actions/runs/35916843156) 成功；[tag Release](https://github.com/Develata/RSS-Reader/actions/runs/35916843160) 第二次运行 7/7 job 成功。首次 Android 失败原因和重跑边界见上。
- `node --check scripts/browser/rssr_small_viewport_assertions.mjs`、`git diff --check`：通过；用 CI 原 Web bundle 重跑 `scripts/run_static_web_small_viewport_smoke.sh --release --skip-build` 默认主题：通过。
- 隔离 Ubuntu 24.04 容器用上一版真实 ELF 预检 `prepare_linux_deb.sh`：成功生成 `0.1.17` 与动态库 `Depends`；该预检不作为新程序启动证据。
- Release Linux job：实际安装本版 `.deb`，Xvfb 下以普通用户启动两次；检查中文和空格路径中的两个数据库、`0700` 目录权限、第二次启动可读取第一次写入的数据，以及未在 `/usr/bin` 生成旧数据目录。
- `gh release download v0.1.17` 下载 11 个公开附件；逐一比对文件大小和 SHA-256 与 GitHub Release API，11/11 一致。`dpkg-deb -f` 对下载的 `.deb` 复核版本与依赖字段；`git ls-remote` 复核远端 annotated tag 指向完整发布 commit。
- Release README/使用指南版本映射与 `git diff --check`：通过。

### 手工验收

- Android 真机系统返回、长按文字选择、下拉刷新和图片手势；macOS 实机交互；Windows 原生右键菜单实际弹出：本轮未执行。CI 构建、浏览器模拟与 Linux Xvfb 不能代替这些设备路径。

## 结果

- `v0.1.17` 已正式发布。Linux `.deb` 的旧写权限问题和缺动态库依赖问题，已有本版包及普通用户启动测试的证据；不把它外推为所有发行版或桌面环境兼容。
- 本轮没有新的性能测量，不宣称性能收益。本记录与文档更新位于 tag 之后的主线提交，不改变已发布源码。

## 风险与后续事项

- `.deb` 的最低库版本来自 Ubuntu 24.04 构建环境，旧版发行版必须先核对 `Depends`。若旧版以管理员身份把数据写在 `/usr/bin/RSS-Reader/`，新版不会自动迁移，须备份后手工复制并修正所有权。
- Android/macOS/Windows 尚缺上述实机交互验收。发布说明保留这些缺口。
- Android 的 Gradle 下载链偶发证书错误曾导致发布首次运行失败；重跑成功，但尚未形成稳定的上游修复证据。

## 给下一位 Agent 的备注

- 不要移动或重建 `v0.1.17` tag；此后的文档与代码提交都属于 `main`，不会改变本版附件。
- 复核发布身份时使用 `git ls-remote origin refs/tags/v0.1.17 'refs/tags/v0.1.17^{}'`、Release API digest 与同一 tag 的 workflow run，而非旧版产物。
