# US1 性能检查模板：MVP 刷新与阅读

## 记录信息

- 验证日期：`2026-03-25`
- 验证环境：`WSL Ubuntu + Dioxus Desktop (WSLg)`
- 构建方式：`cargo fmt --all` / `cargo test --workspace` / `cargo check -p rssr-app --target wasm32-unknown-unknown` / `cargo run -p rssr-app`
- 数据规模：`至少 1 个真实 feed，已完成添加订阅、刷新、阅读与离线缓存阅读`

## 建议前置条件

- 本地数据库已完成迁移。
- 至少准备 1 个稳定可访问的 feed。
- 本地已有可打开的文章正文，且至少包含 1 篇 HTML 正文文章。
- 关闭明显干扰测量的后台任务，尽量保持网络条件稳定。

## 建议记录方式

- 每个检查项至少执行 3 次，记录平均值和最大值。
- 如无精确计时工具，可使用系统录屏或秒表，重点记录是否出现明显停顿。
- 若结果受网络影响明显，需在备注中注明。

## 检查步骤

1. 冷启动应用并进入首页，记录从启动到可点击导航的时间。
2. 从订阅页添加 1 个有效 feed，记录从提交到列表出现该订阅的时间。
3. 对已有订阅执行“刷新全部”，记录从点击到文章列表可见新文章的时间。
4. 在无更新的情况下再次刷新相同订阅，确认不会重复写入，并记录操作是否出现明显卡顿。
5. 进入文章页，在默认排序下连续滚动列表 10 秒，记录是否出现明显掉帧或输入延迟。
6. 打开一篇含完整 HTML 正文的文章，记录从点击列表项到正文可读展示的时间。
7. 在阅读页返回文章列表，再次打开另一篇文章，记录二次进入阅读页的时间。
8. 断网后打开已缓存文章，确认仍可本地阅读，并记录是否有明显等待。

## 结果记录

| 项目 | 第 1 次 | 第 2 次 | 第 3 次 | 平均/结论 | 备注 |
|------|---------|---------|---------|-----------|------|
| 冷启动到可导航 | 未精确计时 | 未精确计时 | 未精确计时 | 通过 | 无明显长时间停顿 |
| 添加订阅反馈 | 未精确计时 | 未精确计时 | 未精确计时 | 通过 | 可成功添加并完成首次刷新 |
| 首次刷新完成 | 未精确计时 | 未精确计时 | 未精确计时 | 通过 | 文章可进入阅读页 |
| 无更新刷新 | 未单独测量 | 未单独测量 | 未单独测量 | 部分验证 | 自动化已覆盖去重，GUI 未单独计时 |
| 文章列表滚动 | 已观察 | 已观察 | 已观察 | 通过 | 当前样本下无明显卡顿 |
| 首次打开阅读页 | 未精确计时 | 未精确计时 | 未精确计时 | 通过 | 正文可读，完整 HTML 展示正常 |
| 再次打开阅读页 | 未精确计时 | 未精确计时 | 未精确计时 | 通过 | 再次进入可正常显示 |
| 离线打开缓存文章 | 已验证 | 已验证 | 已验证 | 通过 | 断网后已缓存文章仍可打开 |

## 通过标准

- 冷启动、订阅添加、刷新和阅读形成连续闭环，过程中无明显长时间停顿。
- 无更新刷新不会制造重复文章，也不会产生明显噪音或阻塞。
- 阅读页能在本地缓存存在时稳定打开，不依赖在线正文回源。
- 结果满足 [spec.md](/home/develata/gitclone/RSS-Reader/specs/001-minimal-rss-reader/spec.md) 中 `SC-001` 和 `SC-003` 的主观响应性要求。

## 结论

- 状态：`通过`
- 主要观察：`已在真实桌面 UI 会话中完成添加订阅、刷新、阅读和离线缓存阅读闭环；当前样本下无明显卡顿。`
- 风险与后续动作：`未记录精确耗时，无更新刷新场景的 GUI 体验主要依赖自动化去重覆盖；如需发布级性能结论，仍建议补录定量数据。`
